import React, { Component } from 'react';
import { View, Text } from 'react-native';

export default (official) => {
  const {
    id,
    first_name,
    last_name
  } = official;
  return (
    <View>
      <Text>yolo</Text>
    </View>
  );
};
